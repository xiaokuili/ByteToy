
import { Render, RenderConfig, DisplayFormat } from "@/lib/types";


const SearchResultsComponent: Render = ({ config }: { config: RenderConfig }): React.ReactNode => {
    return <div>SearchResults</div>;
};

export default SearchResultsComponent;